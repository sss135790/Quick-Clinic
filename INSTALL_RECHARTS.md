# Install Recharts for Charts

The earnings dashboard uses Recharts for data visualization. To enable the charts, install recharts:

```bash
npm install recharts
```

After installation, the earnings charts will work automatically.

